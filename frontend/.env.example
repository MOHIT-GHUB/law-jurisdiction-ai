# Backend API URL.
# Leave EMPTY in development — the Vite dev server proxies /auth, /conversations,
# and /ws to the backend (see vite.config.ts), which avoids CORS entirely.
# Set this to your backend's origin only for a production build.
VITE_API_URL=
