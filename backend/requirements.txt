# ─────────────────────────────────────────────────────────────
#  LexAI Backend — Python Dependencies
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Web framework + ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0          # [standard] adds websocket support (required)

# Database — async SQLAlchemy + PostgreSQL driver
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0                    # async Postgres driver (asyncpg in DATABASE_URL)

# Config + validation
pydantic-settings==2.2.1
pydantic==2.7.0

# Auth — JWT tokens + password hashing
python-jose[cryptography]==3.3.0   # JWT encode/decode (utils/auth.py)
passlib[bcrypt]==1.7.4             # bcrypt password hashing (utils/auth.py)

# Redis — async client for caching
redis[asyncio]==5.0.4              # redis_client.py

# HTTP client — for calling Congress.gov, Cornell LII, CourtListener, Google Maps
httpx==0.27.0                      # tools/*.py

# LangGraph + LangChain — multi-agent framework (agents/)
langchain-openai==0.1.6            # ChatOpenAI in all agents
langgraph==0.1.5                   # StateGraph, MemorySaver (agents/graph.py)
langchain-core==0.2.5              # SystemMessage, HumanMessage, AIMessage

# PDF generation — choose one and uncomment:
reportlab==4.1.0                   # Option A: programmatic PDF (utils/pdf_export.py)
# weasyprint==61.2                 # Option B: HTML → PDF (easier to style)
